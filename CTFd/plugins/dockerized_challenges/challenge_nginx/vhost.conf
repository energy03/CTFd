server {{
    listen 80;
    server_name {server_name};
    include {token_variables};
    location / {{
        if ($cookie_{chall}_token != $expected_token_{chall}) {{
	        return 403;
        }}
        proxy_pass http://127.0.0.1:{port};
    }}
}}
